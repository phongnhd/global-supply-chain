{
  "name": "global-supply-chain",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo: Next.js + Node.js + Sui Move (supply chain)",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev:web": "npm run dev -w frontend",
    "dev:api": "npm run dev -w backend",
    "build": "npm run build -ws --if-present",
    "lint": "npm run lint -ws --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.95.1",
    "@google-cloud/vision": "^5.3.6",
    "axios": "^1.16.0",
    "form-data": "^4.0.5",
    "fs": "^0.0.1-security",
    "kubo-rpc-client": "^6.1.0"
  }
}
